{
  "dependencies": {
    "@observablehq/notebook-kit": "^1.5.0"
  },
  "scripts": {
    "preview": "notebooks preview --root nbks --template resources/custom.tmpl",
    "build": "notebooks build --root nbks --out ../dist --template resources/custom.tmpl -- nbks/*.html",
    "download": "notebooks download https://observablehq.com/@shadoof/inapm0_shared > nbks/inapm0_shared.html"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
